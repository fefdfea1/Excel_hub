/**
 * 사이트 전체 설정.
 * 여기 있는 값만 고치면 되고, 다른 코드는 건드릴 일이 없습니다.
 */
export const site = {
  /** 브라우저 탭과 헤더에 나오는 사이트 이름 */
  name: '엑셀 템플릿',

  /** 목록 페이지 맨 위 문구 */
  headline: '엑셀 템플릿',
  tagline:
    '무료 엑셀 템플릿입니다. 문제점·개선사항이나 필요한 템플릿 요청은 사이트 하단에 적어주세요',

  /** 검색 결과와 공유 카드에 쓰이는 설명 */
  description:
    '실무에서 바로 쓰는 무료 엑셀 템플릿 모음. 견적서, 재고관리, 자금관리, 설비점검 등 서식을 무료로 받아가세요.',

  /** 배포한 실제 주소. 공유 카드와 댓글 스레드 기준이 됩니다. 끝에 / 를 붙이지 마세요. */
  url: 'https://example.com',

  /** 맨 아래 한 줄 */
  colophon: '엑셀 템플릿 · 무료로 사용하실 수 있습니다.',

  comments: {
    /**
     * disqus.com 에서 사이트를 등록하면 받는 고유 이름.
     * 빈 문자열로 두면 댓글 영역에 안내 문구만 나옵니다.
     */
    shortname: 'exelltemplate',

    /**
     * 댓글 스레드 식별자. 사이트 전체가 이 스레드 하나를 같이 씁니다.
     * 한 번 정하면 바꾸지 마세요. 바꾸면 기존 댓글이 사라진 것처럼 보입니다.
     */
    identifier: 'excel-templates-global',

    /** 댓글 영역 제목과 안내 문구 */
    heading: '문제점 · 개선사항 · 추가요청',
    hint: '문제점이나 개선할 점은 어떤 템플릿의 어느 부분인지 함께 적어주세요. 필요한 템플릿이 있으면 어떤 일에 쓰실 건지 적어주시면 만들어 보겠습니다.',
  },

  /** 상세 페이지 미리보기 자동 넘김 간격(초). 0 이면 끕니다. */
  autoplaySeconds: 0,

  /**
   * 목록 정렬 방향. 폴더 이름 앞 숫자를 기준으로 합니다.
   *   'asc'  작은 번호부터 (01, 02, 03 …) — 원본 폴더 순서 그대로
   *   'desc' 큰 번호부터 (09, 08, 07 …) — 새로 넣은 것이 위로
   */
  listOrder: 'asc' as 'asc' | 'desc',
} as const;
