/**
 * 사이트 전체 설정.
 * 여기 있는 값만 고치면 되고, 다른 코드는 건드릴 일이 없습니다.
 */
export const site = {
  /** 브라우저 탭과 헤더에 나오는 사이트 이름 */
  name: '엑셀 템플릿',

  /** 목록 페이지 맨 위 문구 */
  headline: '무료 엑셀 템플릿 모음',
  tagline:
    '무료 엑셀 템플릿입니다. 문제점·개선사항이나 필요한 템플릿 요청은 사이트 하단에 적어주세요',

  /**
   * 검색 결과 첫 줄에 나오는 제목.
   * 사이트 이름보다 조금 길게, 여기서 무엇을 받아갈 수 있는지 드러나게 씁니다.
   * 30자 안팎이면 검색 결과에서 잘리지 않습니다.
   */
  titleDefault: '무료 엑셀 템플릿 모음 · 견적서 재고관리 자금관리 서식',

  /** 검색 결과와 공유 카드에 쓰이는 설명 */
  description:
    '실무에서 바로 쓰는 무료 엑셀 템플릿 모음. 견적서, 재고관리, 자금관리, 설비점검 등 서식을 무료로 받아가세요.',

  /**
   * 이 사이트를 한마디로 설명하는 낱말들. 검색 엔진에 힌트로 넘어갑니다.
   * 욕심내서 잔뜩 넣기보다, 실제로 여기 있는 것만 열 개 안쪽으로 적습니다.
   */
  keywords: [
    '무료 엑셀 템플릿',
    '엑셀 서식',
    '엑셀 양식 다운로드',
    '견적서 양식',
    '거래명세서 양식',
    '재고관리 엑셀',
    '자금일보',
    '차량운행일지',
    '설비점검표',
  ],

  /**
   * 카카오톡·페이스북 등에 주소를 붙였을 때 뜨는 이미지. public 폴더 기준 경로입니다.
   * `npm run og` 로 다시 만들 수 있고, 직접 그린 1200×630 이미지로 바꿔도 됩니다.
   */
  ogImage: '/og.png',

  /** 배포한 실제 주소. 공유 카드와 댓글 스레드 기준이 됩니다. 끝에 / 를 붙이지 마세요. */
  url: 'https://excelhub-tau.vercel.app',

  /** 맨 아래 한 줄 */
  colophon: '엑셀 템플릿 · 무료로 사용하실 수 있습니다.',

  /**
   * 구글 애널리틱스 측정 ID. analytics.google.com 에서 만들면 받습니다.
   * `G-` 로 시작하는 열 자리 남짓한 코드입니다.
   *
   * 채워 넣으면 어떤 템플릿을 몇 번 받아 갔는지 집계됩니다.
   * 숫자는 애널리틱스 화면에서만 보이고, 이 사이트에는 나오지 않습니다.
   * 비워두면 애널리틱스를 아예 넣지 않습니다.
   */
  googleAnalyticsId: 'G-SPMKHS4YZ4' as string,

  /**
   * 구글 애드센스.
   *
   * `client` 는 애드센스 계정 하나에 하나뿐인 게시자 ID 입니다. 이 값이
   * 비어 있으면 광고 스크립트를 아예 넣지 않습니다.
   *
   * `sideSlots` 는 본문 양옆에 세울 광고 단위 ID 입니다. 애드센스에서
   * '디스플레이 광고' 단위를 좌·우용으로 하나씩 만들면 열 자리 남짓한
   * 숫자를 받는데, 그것을 적으면 됩니다. 비워두면 그쪽에는 광고가
   * 나오지 않고, 화면이 좁을 때도 나오지 않습니다.
   *
   * `bottomSlot` 은 본문 맨 아래 가로 배너입니다. 좁은 화면에서도 나오므로
   * 휴대폰으로 들어온 사람에게 보이는 것은 이 자리 하나입니다.
   */
  adsense: {
    client: 'ca-pub-4703835297829017' as string,
    sideSlots: {
      left: '' as string,
      right: '' as string,
    },
    bottomSlot: '' as string,
  },

  comments: {
    /**
     * disqus.com 에서 사이트를 등록하면 받는 고유 이름.
     * 빈 문자열로 두면 댓글 영역에 안내 문구만 나옵니다.
     */
    shortname: 'exelltemplate',

    /**
     * 댓글 스레드 식별자. 사이트 전체가 이 스레드 하나를 같이 씁니다.
     * 한 번 정하면 바꾸지 마세요. 바꾸면 기존 댓글이 사라진 것처럼 보입니다.
     */
    identifier: 'excelhub-global-v2',

    /** 댓글 영역 제목과 안내 문구 */
    heading: '문제점 · 개선사항 · 추가요청',
    hint: '문제점이나 개선할 점은 어떤 템플릿의 어느 부분인지 함께 적어주세요. 필요한 템플릿이 있으면 어떤 일에 쓰실 건지 적어주시면 만들어 보겠습니다.',
  },

  /** 상세 페이지 미리보기 자동 넘김 간격(초). 0 이면 끕니다. */
  autoplaySeconds: 0,

  /**
   * 목록 정렬 방향. 폴더 이름 앞 숫자를 기준으로 합니다.
   *   'asc'  작은 번호부터 (01, 02, 03 …) — 원본 폴더 순서 그대로
   *   'desc' 큰 번호부터 (09, 08, 07 …) — 새로 넣은 것이 위로
   */
  listOrder: 'asc' as 'asc' | 'desc',
} as const;
